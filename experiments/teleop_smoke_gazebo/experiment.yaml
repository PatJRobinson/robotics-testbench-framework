apiVersion: sim.platform/v0.1
kind: Experiment

metadata:
  name: teleop_smoke_gazebo
  description: >
    Minimal interactive experiment used to validate AMR teleoperation against
    the Gazebo realisation of the warehouse teleop scenario.

spec:
  mode: interactive

  appRef: amr_teleop
  realisationRef: warehouse_teleop@gazebo

  successCriteria:
    description: >
      The application successfully publishes velocity commands to a compatible
      realisation, and the robot responds to teleoperation in simulation.
    requires:
      - /cmd_vel accepted by realisation
      - robot motion observable
      - /clock available to application

  optionalChecks:
    - /odom available
    - /tf available

  perturbations: []

  outputs:
    record:
      - runtime_logs
      - selected_realisation
      - selected_app
      - experiment_result

  tags:
    - smoke_test
    - teleop
    - interactive
    - gazebo
